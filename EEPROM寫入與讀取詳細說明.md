# EEPROM 寫入與讀取詳細說明

## 📋 功能概述

EEPROM (Electrically Erasable Programmable Read-Only Memory) 是 Arduino 內建的非揮發性記憶體，用於儲存不會因為斷電而遺失的資料。

本韌體 F8 功能允許 PC 端透過藍牙發送命令，將資料寫入 Arduino 的 EEPROM，並在選單中讀取顯示。

---

## 🔧 硬體基礎

### Arduino UNO 的 EEPROM
- **容量**：1024 位元組（起始位址 0x00 至 0x3FF）
- **壽命**：~100,000 次讀寫循環
- **存取速度**：較慢（數毫秒）
- **特點**：斷電不遺失資料

### 使用的地址
| 地址 | 內容 | 大小 | 用途 |
|------|------|------|------|
| 0 | 十進位數值 | 1 位元組 | 儲存使用者寫入的資料 (0-255) |
| 1 | 簽名 (0xAA) | 1 位元組 | 初始化標記，防止讀取未初始化的亂數 |

---

## 📨 PC 端寫入流程

### 流程圖
```
┌─────────────────┐
│ PC 端發送資料   │
│ "WRITE 123\n"   │
└────────┬────────┘
         │
         ▼
┌────────────────────────────────┐
│ Arduino 藍牙接收              │
│ 將字符儲存至 receivedData[]   │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ 偵測換行符號 (\n 或 \r)        │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ 資料清理                        │
│ - 去除前導空格                │
│ - 去除末尾空格                │
│ - 確保 null 結尾               │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ 搜尋 "WRITE" 關鍵字            │
│ (寬鬆匹配: strstr)             │
└────────┬───────────────────────┘
         │
    ┌────┴────┐
    │          │
   找到       未找到
    │          │
    ▼          ▼
  ┌─────┐   ┌──────┐
  │下一步│   │回傳 │
  └──┬──┘   │ERR  │
     │      └──────┘
     ▼
┌────────────────────────────────┐
│ 在字串中搜尋數字               │
│ 跳過 "WRITE" 和非數字字符      │
└────────┬───────────────────────┘
         │
    ┌────┴────┐
    │          │
   找到       未找到
    │          │
    ▼          ▼
  ┌──────┐  ┌──────┐
  │下一步 │  │回傳 │
  └──┬───┘  │ERR  │
     │      └──────┘
     ▼
┌────────────────────────────────┐
│ 使用 atoi() 轉換為整數         │
│ (自動停止於非數字字符)         │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ 驗證範圍 (0-255)              │
└────────┬───────────────────────┘
         │
    ┌────┴────┐
    │          │
  在範圍內    超出範圍
    │          │
    ▼          ▼
  ┌──────┐  ┌──────────┐
  │下一步 │  │回傳 ERR  │
  └──┬───┘  └──────────┘
     │
     ▼
┌────────────────────────────────┐
│ 呼叫 writeEEPROM(value)        │
│ - 寫入數值至地址 0             │
│ - 寫入簽名至地址 1             │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ 回傳 "ACK"                     │
│ 連線保持 Connected            │
└────────────────────────────────┘
```

---

## 📝 寫入實現細節

### 程式碼位置
- **檔案**：`src/main.cpp`
- **藍牙接收**：行 973-995（WRITE 命令處理）
- **EEPROM 寫入**：行 1072-1082（writeEEPROM 函式）

### 命令接收處理（行 973-995）
```cpp
if (strstr(receivedData, "WRITE") != NULL) {
    // 在整個字串中搜尋第一個數字
    char* valuePtr = receivedData;
    while (*valuePtr != '\0' && !isdigit((unsigned char)*valuePtr)) {
        valuePtr++;
    }
    
    // 如果找到數字，提取並驗證
    if (*valuePtr != '\0') {
        int value = atoi(valuePtr);  // 提取數值
        
        // 驗證範圍 (0-255)
        if (value >= 0 && value <= 255) {
            writeEEPROM(value);       // ⭐ 寫入
            Serial.println("ACK");
            
            // 如果在 EEPROM 選單中，更新顯示
            if (currentMenu == MENU_EEPROM && inSubMenu) {
                displayEEPROMValue();
            }
        } else {
            Serial.println("ERR");
        }
    } else {
        Serial.println("ERR");
    }
}
```

### EEPROM 寫入函式（行 1072-1082）
```cpp
void writeEEPROM(int value) {
    if (value >= 0 && value <= 255) {
        // ⭐ 寫入數值至地址 0
        EEPROM.write(EEPROM_ADDR_VALUE, value);
        
        // ⭐ 寫入簽名至地址 1（標記為已初始化）
        EEPROM.write(EEPROM_ADDR_SIGNATURE, EEPROM_SIGNATURE);
        
        // 更新記憶體快取
        eepromValue = value;
        eepromValid = true;
    } else {
        eepromValid = false;
    }
}
```

---

## 📖 EEPROM 讀取流程

### 流程圖
```
┌──────────────────┐
│ 使用者進入       │
│ EEPROM 選單      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 呼叫 readEEPROM()│
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────┐
│ 讀取簽名（地址 1）            │
│ signature = EEPROM.read(1)   │
└────────┬─────────────────────┘
         │
    ┌────┴────┐
    │          │
 簽名正確    簽名錯誤
 (0xAA)     (不是 0xAA)
    │          │
    ▼          ▼
  ┌─────┐  ┌──────┐
  │下一步│  │返回 0 │
  └──┬──┘  │標記無效│
     │      └──────┘
     ▼
┌──────────────────────────────┐
│ 讀取數值（地址 0）            │
│ value = EEPROM.read(0)       │
└────────┬─────────────────────┘
         │
    ┌────┴────┐
    │          │
  有效        無效
 (0-255)   (不在範圍)
    │          │
    ▼          ▼
  ┌─────┐  ┌──────┐
  │顯示  │  │顯示  │
  │數值  │  │ERROR │
  │(綠色)│  │(紅色)│
  └─────┘  └──────┘
```

### 讀取函式（行 1085-1101）
```cpp
int readEEPROM() {
    int signature = EEPROM.read(EEPROM_ADDR_SIGNATURE);
    
    // 檢查簽名是否正確（表示已初始化）
    if (signature == EEPROM_SIGNATURE) {
        int value = EEPROM.read(EEPROM_ADDR_VALUE);
        
        // 驗證讀取的數值在有效範圍內
        if (value >= 0 && value <= 255) {
            eepromValid = true;
            return value;
        }
    }
    
    // 若簽名不符或讀取失敗
    eepromValid = false;
    return 0;
}
```

---

## 🎯 容錯特性

### 支援的 WRITE 命令格式

所有以下格式均可正確處理：

```
標準格式:
WRITE 123        ✅ 標準

多空格:
WRITE  123       ✅ 雙空格
WRITE   123      ✅ 三空格

無空格:
WRITE123         ✅ 直接連接

前導空格:
 WRITE 123       ✅ 命令前有空格
  WRITE 123      ✅ 多個前導空格

末尾空格:
WRITE 123        ✅ 命令後有空格
WRITE 123        ✅ 多個末尾空格

亂碼混雜（藍牙干擾):
WRITE❌WRITE     ✅ 自動恢復
LOAD❌WRITE 100  ✅ 提取正確數值
```

### 驗證規則

| 驗證項目 | 規則 | 結果 |
|---------|------|------|
| 數值範圍 | 必須 0-255 | 超出 → ERR |
| 命令格式 | 只需含 "WRITE" | 否 → ERR |
| 數字搜尋 | 動態搜尋 | 未找到 → ERR |

---

## 📊 測試與驗證

### 測試 1：標準寫入
```
命令: WRITE 123
預期: ACK → EEPROM 儲存 123
驗證: 進入 EEPROM 選單 → 顯示 "Stored Value: 123"（綠色）
```

### 測試 2：邊界值
```
WRITE 0      ✅ ACK（最小值）
WRITE 255    ✅ ACK（最大值）
WRITE 256    ❌ ERR（超出範圍）
WRITE -1     ❌ ERR（負數）
```

### 測試 3：容錯格式
```
WRITE  123   ✅ ACK（多空格自動處理）
WRITE123     ✅ ACK（無空格自動處理）
WRITE❌WRITE ✅ ACK（亂碼自動恢復）
```

### 測試 4：簽名驗證
```
步驟 1: WRITE 200         → ACK
        EEPROM[0] = 200
        EEPROM[1] = 0xAA

步驟 2: 重新啟動 Arduino

步驟 3: 進入 EEPROM 選單
        讀取 EEPROM[1]
        ✓ 簽名 = 0xAA (初始化成功)
        讀取 EEPROM[0]
        ✓ 數值 = 200
        
顯示: "Stored Value: 200"（綠色）✅
```

---

## 🛡️ 故障排除

### 問題 1：EEPROM 無法讀取
**症狀**：進入 EEPROM 選單顯示 "ERROR: EEPROM"（紅色）

**可能原因**：
- 首次使用未初始化
- 簽名 (0xAA) 遺失

**解決方法**：
1. 透過藍牙發送 WRITE 命令初始化：`WRITE 100`
2. 等待收到 ACK
3. 重新進入 EEPROM 選單

### 問題 2：WRITE 命令總是回傳 ERR
**症狀**：發送任何 WRITE 命令都收到 ERR

**可能原因**：
- 數值超出 0-255 範圍
- 藍牙傳輸有誤
- 命令格式錯誤

**解決方法**：
1. 確認數值在 0-255 範圍內
2. 使用序列埠監視器檢查接收的資料
3. 重新發送命令

### 問題 3：重新啟動後資料遺失
**症狀**：斷電重啟後 EEPROM 資料消失

**可能原因**：
- 簽名驗證失敗
- 簽名 (0xAA) 未被寫入

**解決方法**：
1. 確認已成功發送 WRITE 命令（收到 ACK）
2. 檢查是否有使用者端的錯誤代碼
3. 聯繫技術支援

---

## ⚡ 效能考慮

### 寫入速度
- **單次寫入時間**：~3.3 毫秒
- **實際體驗**：幾乎瞬間完成

### 讀取速度
- **單次讀取時間**：~0.1 毫秒
- **快速恢復**：開機啟動時自動讀取

### 壽命考慮
- **設計壽命**：~100,000 次循環
- **實際使用**：日常使用不會達到限制
- **安全建議**：避免在迴圈中頻繁寫入

---

## 📚 相關檔案參考

- **程式碼**：`src/main.cpp`（行 973-1101）
- **規格文件**：`FirmwareSpec.md`
- **快速參考**：`藍牙資料格式快速參考.md`
- **測試指南**：`測試指南.md`（F8 測試）

---

## 📞 技術支援

如有問題或建議，請參考：
- FirmwareSpec.md - 完整韌體規格
- 測試指南.md - 系統測試程序
- README.md - 常見問題與解決方案

---

**版本**：v2.0  
**更新日期**：2025-11-21  
**對應韌體版本**：main.cpp v10+
